# FEW-AI mobile menu fix

These HTML files include a hamburger mobile menu for screens below the `md` Tailwind breakpoint.

Upload these files to the root of your GitHub Pages repository, replacing the existing files with the same names.
Do not rename `index.html`.
